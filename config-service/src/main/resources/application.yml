server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MahdiAlouane/tp4-homework-config
      overrideSystemProperties: false
      overrideNone: true
